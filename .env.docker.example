# =============================================================================
# Ambiente de Desenvolvimento e Produção - Agenda de Músicos (EXEMPLO)
# Copie este arquivo para .env.docker e preencha os valores reais
# =============================================================================

# =============================================================================
# Backend Django (Configurações com prefixo DB_*)
# =============================================================================

# Chave secreta do Django (gerada dinamicamente em produção)
DB_SECRET_KEY=django-insecure-$(python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")

# Modo de desenvolvimento (True para local, False para produção)
DB_DEBUG=True

# Hosts permitidos para acessar a aplicação
# Desenvolvimento: localhost,127.0.0.1
# Produção: gigflowagenda.com.br,www.gigflowagenda.com.br
DB_ALLOW_HOSTS=localhost,127.0.0.1

# URL da secret admin (gerada dinamicamente)
DB_ADMIN_URL=admin-secret-$(python -c "import secrets; print(secrets.token_urlsafe(16))")

# URL do banco de dados PostgreSQL
# Desenvolvimento (local-up.sh): postgresql://agenda:agenda@localhost:5433/agenda
# Desenvolvimento (docker-compose): postgresql://agenda:agenda@db:5432/agenda
# Produção: postgresql://agenda:SUA_SENHA_AQUI@db:5432/agenda
DB_DATABASE_URL=postgresql://agenda:agenda@localhost:5433/agenda

# Configurações CORS (permite acesso do frontend)
DB_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
DB_CORS_CREDENTIALS=True
DB_CSRF_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Configurações CSP (Content Security Policy)
DB_CSP_HEADER=default-src 'self'; img-src 'self' data: https:; script-src 'self' 'nonce-{nonce}'; style-src 'self'; connect-src 'self'

# Google OAuth - Client ID (backend)
# Obter em: https://console.cloud.google.com/apis/credentials
DB_GOOGLE_CLIENT_ID=SEU_GOOGLE_CLIENT_ID

# URL do frontend (para links de verificação e convites)
DB_FRONTEND_URL=http://localhost:5173

# Email (SMTP - Gmail com App Password)
DB_EMAIL_HOST=smtp.gmail.com
DB_EMAIL_PORT=587
DB_EMAIL_USE_TLS=True
DB_EMAIL_HOST_USER=seu-email@gmail.com
DB_EMAIL_HOST_PASSWORD=SUA_APP_PASSWORD
DB_DEFAULT_FROM_EMAIL=Agenda Musicos <seu-email@gmail.com>

# =============================================================================
# Frontend Vite (Variáveis com prefixo VITE_*)
# =============================================================================

# URL da API do backend
# Desenvolvimento (local-up.sh): http://localhost:8000/api
# Desenvolvimento (docker-compose): http://localhost:8001/api
# Produção: /api (nginx proxy)
VITE_API_URL=http://localhost:8000/api

# Google OAuth - Client ID (frontend)
# Obter em: https://console.cloud.google.com/apis/credentials
# IMPORTANTE: Deve ser o mesmo valor usado no backend (DB_GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=SEU_GOOGLE_CLIENT_ID

# Payment Service API (opcional)
VITE_PAYMENT_API_URL=/payments/api

# =============================================================================
# Variáveis Docker Compose (produção)
# =============================================================================

# PostgreSQL (produção)
POSTGRES_DB=agenda
POSTGRES_USER=agenda
POSTGRES_PASSWORD=SUA_SENHA_AQUI

# Variáveis usadas pelo docker-compose.prod.yml
# URL da API do frontend em produção (usada no build do Docker)
VITE_API_URL=/api
