# Configurações da Aplicação Django - Agenda de Músicos
# IMPORTANTE: Copie este arquivo para .env e preencha com os valores corretos

# Gere uma SECRET_KEY única e forte usando:
# python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=sua-chave-secreta-unica-aqui

# NUNCA use DEBUG=True em produção!
# Desenvolvimento: DEBUG=True
# Produção: DEBUG=False
DEBUG=False

# Liste os domínios/IPs permitidos separados por vírgula
# Desenvolvimento: localhost,127.0.0.1
# Produção: seu-dominio.com,api.seu-dominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# URL do Banco de Dados
# SQLite (dev): sqlite:///db.sqlite3
# PostgreSQL (prod): postgresql://user:password@localhost:5432/dbname
DATABASE_URL=sqlite:///db.sqlite3

# CORS - Origens permitidas (separadas por vírgula)
# Exemplo: https://frontend.com,https://app.frontend.com
# CORS_ALLOWED_ORIGINS=

# Configurações opcionais de segurança (recomendado para produção com HTTPS)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# SECURE_HSTS_PRELOAD=True
